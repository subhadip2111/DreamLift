/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','as1.ftcdn.net','res.cloudinary.com'], 

      
      },
      crossOrigin: 'anonymous',
};

export default nextConfig;
